# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_release-main --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: release-main

on:
  push:
    branches:
      - master
      - main

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd Push PublishGithubRelease'
        run: ./build.cmd Push PublishGithubRelease
        env:
          PersonalAccessToken: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
      - uses: actions/upload-artifact@v1
        with:
          name: test-results
          path: artifacts/test-results
      - uses: actions/upload-artifact@v1
        with:
          name: packages
          path: artifacts/packages
